{
  "id": "mail-intelligence",
  "name": "Mail Intelligence",
  "description": "OpenClaw plugin tool for structured mail classification.",
  "version": "0.1.0",
  "configSchema": {
    "type": "object",
    "additionalProperties": false,
    "properties": {
      "defaultModel": {
        "type": "string",
        "description": "Optional default backend model override for mail intelligence classification"
      },
      "promptVersion": {
        "type": "string",
        "description": "Optional prompt version label for traceability"
      }
    }
  },
  "contracts": {
    "tools": ["mail-classify"]
  }
}
